/////////////////////////////////////////////// /* Import Packages */ //////////////////////////////////////////////////////////
const functions = require('./sqlFunctions.js');
const customer = require ('./bamazonCustomer.js');
const async = require('async');

/////////////////////////////////////////////// /* Establish Connection to Database */ //////////////////////////////////////////////////////////


/////////////////////////////////////////////// /* Code Executions */ //////////////////////////////////////////////////////////


functions.connection.connect((error) => {

  if (error) throw error; // Throws an Error if Connection Fails

  async.waterfall([
    functions.createDb,
    functions.createTable,
    functions.createMockData,
    customer,
  ], error => {if (error) throw error});


}); // End of Database Creation

//////////////////////////////////////////////// /* Imports*/ //////////////////////////////////////////////////////////
